name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: [ubuntu-22.04, ubuntu-22.04-arm]

    steps:
    - uses: actions/checkout@v4
    - name: make
      run: make